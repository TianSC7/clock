<Window x:Class="PersonalAssistant.Views.RestOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="休息时间" Height="450" Width="800"
        WindowStyle="None" WindowState="Maximized"
        Topmost="True" ShowInTaskbar="False"
        Background="#E02C3E50" ResizeMode="NoResize">

    <Grid>
        <Border Background="#E02C3E50" CornerRadius="0">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" BlurRadius="30" Opacity="0.5" Color="Black"/>
            </Border.Effect>
        </Border>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Background="#20FFFFFF" CornerRadius="24" Padding="60,50"
                    HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="🌿 休息时间" FontSize="42" FontWeight="Bold"
                               Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,24"/>
                    <TextBlock x:Name="RestTimeDisplay" Text="{Binding TimeDisplay}" FontSize="68" FontWeight="Bold"
                               Foreground="#FF6B6B" HorizontalAlignment="Center"
                               FontFamily="{StaticResource MonoFont}" Margin="0,0,0,28"/>
                    <TextBlock Text="请离开屏幕，放松一下" FontSize="18"
                               Foreground="#AAFFFFFF" HorizontalAlignment="Center" Margin="0,0,0,24"/>
                    <Button Content="紧急情况" FontSize="13" Foreground="#55FFFFFF"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            HorizontalAlignment="Center" Padding="16,6"
                            Click="EmergencyButton_Click"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Window>
